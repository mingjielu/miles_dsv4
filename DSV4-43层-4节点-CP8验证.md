# DeepSeek-V4-Flash 完整 43 层 — 4 节点 CP=8/TP=1/PP=4 验证

在统一镜像上验证**完整 43 层**模型(不是 4 层剪枝)跨 4 节点训练，并行度按要求为
**CP=8 / TP=1 / PP=4**。

镜像：`amdagi/miles-dsv4-flash:rocm7.14-gfx942-cp8mem-20260825-fada`
硬件：4 × 8 MI308X (gfx942)，192 GiB/卡，共 32 卡
节点：c489(Ray head) / f293 / f294 / f2a0
脚本：`porting/run_full_4node.sh`
日志：`c489:/apps/mingjiel/FADA/work/dsv4-full-4node/logs/20260825_161000/train.log`

---

## 结果：PASS

| 指标 | 值 |
|---|---|
| steps completed | **1 / 1** |
| context_parallel_size | **8**（请求 8） |
| tensor_model_parallel_size | **1**（请求 1） |
| pipeline_model_parallel_size | **4**（请求 4） |
| data_parallel_size | 1 |
| expert_model_parallel_size | 8（ETP=1） |
| `allgather_cp` | True |
| 层切分 | **11 + 11 + 11 + 10 = 43** |
| weight syncs | 64 |
| peak allocated / GPU | **77.81 GiB** |
| peak reserved / GPU | **81.82 GiB** |
| memory line coverage | **32 / 32 ranks** |
| OOM / NaN / RCCL timeout / LDS overflow | **0 / 0 / 0 / 0** |
| wall time | 5703 s（95 min） |

Megatron 自己解析出的布局，作为独立确认：

```
using world size: 32, data-parallel size: 1, context-parallel size: 8,
hierarchical context-parallel sizes: None, tensor-model-parallel size: 1,
pipeline-model-parallel size: 4
  allgather_cp .................................... True
```

阶段耗时：权重同步 997 s → log_probs 840 s → train 1780 s。其余是 530 GB torch_dist 重分片与
SGLang 启动。

显存占 192 GiB 的 41%，余量充裕——这与单节点 128K 的结论一致：真正的约束不是权重，而是激活与
分配器碎片，而两者都已被镜像里的显存优化处理过。

### 未验证的部分

`rollout/rewards`、`train/loss`、`train/grad_norm` 全为 **0.0**。这不是故障，而是 GRPO 的算术：一组
样本若 reward 全同，advantage 即为 0，也就没有梯度。`--mode debug_minimal` 下只有 4 个 prompt ×
8 samples，完整模型在单步内答对数学题的概率本就极低。

**因此这次运行证明的是 pipeline、并行布局与显存包线，没有证明收敛。** 脚本现在会在 PASS 时把这一点
显式打印出来，避免被读成"训练正常"。

---

## 43 层为什么需要显式层切分

**43 是质数**，所以没有任何 PP 尺寸能整除它。Megatron 的检查是：

```python
if args.decoder_first_pipeline_num_layers is None and args.decoder_last_pipeline_num_layers is None:
    assert num_layers % args.transformer_pipeline_model_parallel_size == 0, \
        'Number of layers should be divisible by the pipeline-model-parallel size'
```

只要显式给出首/尾 stage 的层数，这条断言就被跳过、改走不均匀切分。首尾固定后中间 stage 均分余数，
所以余数必须能被中间 stage 数整除——PP=4 时 `43 - first - last` 必须是偶数。

采用 **11 / 11 / 11 / 10**，与 Miles 自带的 32-GPU 预设一致（`_get_parallel_config` 里
`total_gpus == 32` 分支）。最后一级少一层，用来抵消它额外承担的 LM head。

脚本会在启动前算出这个切分并校验，切分不合法时立刻失败而不是等 10 分钟后由 Megatron 报错。

注意 Miles 那个预设本身是 **TP=8 / PP=4 / CP=1**（注释标注 MI355X）。本次要求的是 CP=8/TP=1，
所以通过 `--extra-args` 覆盖——Miles 把 `args.extra_args` 拼在命令末尾，argparse 取后者，覆盖成立。
`--sequence-parallel` 留在内置配置里无害：Megatron 在 TP=1 时会自动关掉它并只发一条 warning。

**TP=1 的前提是镜像里的 `block_H` patch**：TP=1 让每卡拿到全部 64 个 attention head，而原版 forward
sparse-MLA kernel 在 gfx942 上编译不了 64 头（`Q_shared` 单独就是 65536 B，占满 LDS）。脚本在
preflight 里直接检查该 patch，缺失就拒绝启动。

---

## 过程中修掉的 5 个脚本缺陷

`run_full_6node.sh` 此前只跑过 preflight、从未真正提交作业，所以它后半段的代码路径全是未验证的。
本次把它改造成 4 节点时，逐个撞了出来：

### 1. 容器创建后立即退出

`DOCKER_RUN` 数组用 `${DOCKER_RUN[*]}` 展平成字符串再交给远程 shell，`'sleep infinity'` 的引号是
本地 shell 语法、不会进入字符串，于是远程收到 `bash -lc sleep infinity`——`sleep` 成了命令、
`infinity` 成了 `$0`：

```
ExitCode=1  Cmd=["-lc","sleep","infinity"]
sleep: missing operand
```

改为在数组元素内嵌引号字符 `"'sleep infinity'"`。同时新增一步：`docker run -d` 成功只代表容器被接受，
之后显式断言它仍在 running 并打印退出码与最后几行日志，而不是等到 ray start 时收到一句
`container ... is not running`。

### 2. `--extra-env-vars` 的 JSON 被三层引号截断

作业曾以这种方式提交：

```bash
ssh host "sudo docker exec c bash -lc \"${JOB}\""
```

而 `JOB` 里含 `--extra-env-vars '{"KEY": "VAL", ...}'`。那些双引号提前终止了 `-lc` 的参数，Miles 收到
一个残缺字符串，`json.loads` 失败后回退到按空格切分，最终：

```
IndexError: list index out of range   (command_utils.py:213, _parse_extra_env_vars)
```

——27 秒即失败，且发生在整个集群已经拉起之后。改为把 job 写成脚本、**经 stdin** 送入容器
（`docker exec -i ... bash -c 'cat > /tmp/job.sh'`），中间再没有任何 shell 解析它。

### 3. Ray 的 agent 端口被邻居容器占用

`ray job submit` 不走 GCS，它 POST 给 dashboard，再由 dashboard 找一个已注册的 job agent。这些机器上
有十多个容器共用 `--network=host`，其中一个跑着默认端口的 Ray：

```
LISTEN 10.235.200.32:52365   users:(("ray::DashboardA",pid=4066422))
LISTEN *:6379                users:(("gcs_server",pid=4065360))
```

于是我们的 agent 抢不到 52365。**症状极具误导性**：GCS 正常、`ray status` 四个节点全部 Active、
`ray.init()` 的集群形状检查通过，只有作业提交失败，报的还是一句与端口无关的话：

```
RuntimeError: 500: No available agent to submit job, please try again later.
```

修法是把 4 个 agent 端口也一起移开（`--dashboard-agent-listen-port` /
`--dashboard-agent-grpc-port` / `--runtime-env-agent-port` / `--metrics-export-port`，基址
`--ray-agent-base`，默认 52465），并在 preflight 里逐节点检查这 6 个端口是否已被占用。

顺带修了一个自伤问题：端口检查原本跑在 `ray stop` 之前，于是脚本第二次运行必然把自己上一轮的
Ray 判成冲突。现在 preflight 里先对目标容器做一次幂等的 `ray stop --force`。

### 4. `--sglang-mem-fraction-static` 有一个很高的硬下界

从 6 节点的 0.43 降到 0.30 看着是保守，实际直接打死引擎：

```
ValueError: Loaded weights leave no GPU memory for the KV cache under --mem-fraction-static=0.3.
Raise above 0.396 (minimum viable = 1 - available/pre = 0.3952)
```

完整 43 层在 sglang-tp/ep 8 下，**权重本身就占 192 GiB 的约 39.5%**，静态池必须先覆盖权重才谈得上
KV cache。这个参数不是用来给训练侧腾地方的——colocate 模式下 rollout 引擎的权重与 KV 池在训练步内
本来就会被 offload。默认改为 0.45。

### 5. 判定逻辑本身的两处误判

首轮完成后脚本报了 `FAIL — 1 RCCL/NCCL timeout`，而实际上一次超时也没发生：

- **RCCL 误报**：日志里会回显提交的完整命令，那是一行极长的文本，同时含 `NCCL_SOCKET_IFNAME` 等环境
  变量与 `--sglang-watchdog-timeout 1800`。宽松的 `NCCL.*timeout` 就在这一行内**跨字段**匹配上了，
  于是每次成功的运行都会被报成超时。改为锚定真实错误串，并排除命令回显行。
- **rank 覆盖率恒为 0**：正则用 `\[Rank N\][^{]*'max_allocated_GB'` 去桥接 rank 号与指标，但指标在
  字典的 `{` 之后，`[^{]*` 永远跨不过去。改为从整行里提取 rank 号，实测 32/32。

两处都属于"判定比被判定对象更容易错"的情形——不修的话，一次干净的运行会被永久报成 FAIL。

---

## 复现

```bash
cd exp/miles_spec_20260817_1430/porting

# 完整 preflight，不启动训练（会打印将要提交的命令）
./run_full_4node.sh --nodes c489,f293,f294,f2a0 --steps 1 --dry-run

# 实跑
./run_full_4node.sh --nodes c489,f293,f294,f2a0 --steps 1

# 端口与邻居冲突时整体平移
./run_full_4node.sh --nodes ... --ray-port 6417 --ray-dashboard 8283 --ray-agent-base 52565

# 叠加 expandable_segments 训练窗口（单节点 128K 上 reserved −2.88 GiB、步时 −9 s）
./run_full_4node.sh --nodes ... --expandable
```

脚本从**主机**运行（它驱动 `docker` 与 `ssh`），不在容器内。preflight 涵盖：可达性、镜像存在、
`block_H`（TP=1 时）、8 卡空闲、6 个 Ray 端口空闲、资产存在且为 43 层、ref-load 路径能由
`model_name` 推导出来。

`--model-name` 必须是 `DeepSeek-V4-Flash-FP8`：Miles 把它定义为
`Literal["DeepSeek-V4-Flash-FP8", "DeepSeek-V4-Flash-FP8-4layer"]`，其他值会被 tyro 直接拒绝；而且
`torch_dist_name = f"{model_name}_torch_dist"`，ref-load 路径正是这样拼出来的。
（`run_full_6node.sh` 里原本写的 `DeepSeek-V4-Flash` 不在该 Literal 内，是个从未被执行到的 bug。）

---

## 遗留风险：磁盘

跑完这一轮后 4 台机器的根分区占用：

| 节点 | / 使用率 |
|---|---|
| c489 | 84% |
| f293 | 94% |
| f294 | 80% |
| **f2a0** | **96%** |

运行期间 raylet 已经开始告警：

```
/tmp/ray/session_... is over 95% full, available space: 35.3667 GB; capacity: 878.648 GB.
Object creation will fail if spilling is required.
```

这一轮没有触发 spilling，所以没有影响结果。但**再跑之前应当先清理**，否则一旦需要 object spilling
就会失败，而且失败点会离真正的原因很远。占用主要来自 `/tmp/ray/session_*` 与 tilelang 的 JIT 缓存。
第二轮显存分析也踩过同一个坑：一次崩溃写出的 GPU core dump 填满了 879 GB 分区，并静默截断了下一轮的
日志（此后 `AMD_GPU_COREDUMP=0` 成为单节点脚本的默认值）。

四个 `miles_dsv4_4node` 容器仍在 running，可直接用 `--skip-bringup` 复用（GPU 已全部释放）。
